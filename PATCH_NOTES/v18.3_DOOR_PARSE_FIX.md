# Out of Sync v1.8.3 — Door Parse Fix

- Исправлен Parse Error в `features/interaction/door.gd`.
- Удалён вызов несуществующей функции `scene_path_is_empty()`.
- Проверка пути сцены теперь выполняется через `target_scene.is_empty()`.
- Сетевая логика перехода из v1.8.2 сохранена.
