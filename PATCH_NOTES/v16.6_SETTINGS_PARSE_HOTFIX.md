# v16.6 — SETTINGS PARSE HOTFIX

- Fixed Godot 4.7.2 parser error in `settings_menu.gd` at the duplicate-key check.
- The `duplicate` local is now explicitly typed as `bool` because `Dictionary`/`Variant` inference cannot determine its type there.
- No gameplay behavior was changed by this hotfix.
