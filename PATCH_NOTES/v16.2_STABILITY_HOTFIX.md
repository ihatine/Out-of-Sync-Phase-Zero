# v16.2 // STABILITY HOTFIX

- Fixed flashlight body disappearing when the flashlight is switched off.
- Fixed held-item replacement in the same inventory slot: delayed holster tweens could hide the newly equipped item.
- Added HUD recreation to ROOM 02 after scene transitions.
- Added mobile controls to ROOM 02.
- Replaced the removed Godot 4.7 `rpc_unreliable()` call with `rpc()` using the existing `@rpc(..., "unreliable", ...)` declaration.
- Improved window stretching/resizing defaults for 16:9 desktop windows without changing the monitor resolution.
